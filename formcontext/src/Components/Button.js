

const Button = ({Submit}) => {


   
    

    return (
        <>
            <button type="submit" onClick={Submit} className="btn btn-primary col-sm-12 mb-1">Submit</button>
        </>
    );
};

export default Button;